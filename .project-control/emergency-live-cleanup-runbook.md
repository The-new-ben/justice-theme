# Emergency Live Cleanup Runbook

Date: 2026-05-10  
Status: CRITICAL - unsafe internal draft pages are live.

## Problem
The following public URLs currently expose internal/publication markers and must not remain public:

- https://jus-tice.co.il/divorce-lawyer/
- https://jus-tice.co.il/consensual-divorce/
- https://jus-tice.co.il/divorce-mediation/
- https://jus-tice.co.il/child-support/
- https://jus-tice.co.il/child-custody/
- https://jus-tice.co.il/divorce-property-division/
- https://jus-tice.co.il/family-dispute-resolution/

## Preferred Fix
1. Open uPress.
2. Pull latest GitHub commit from `main`.
3. Make sure latest commit includes `e17e0fa` or newer.
4. Open the public homepage once.
5. Open `/wp-admin/` once.
6. Clear any site/cache plugin cache if available.
7. Recheck the seven URLs above.

Expected result after the emergency quarantine runs:
- If a previous page existed, old backed-up content is restored.
- If the page was newly generated by the unsafe publisher, it is moved to draft.
- Nothing is deleted.

## Manual Fallback If The Code Quarantine Does Not Run
Use wp-admin:

1. Go to `Pages`.
2. Search each slug:
   - `divorce-lawyer`
   - `consensual-divorce`
   - `divorce-mediation`
   - `child-support`
   - `child-custody`
   - `divorce-property-division`
   - `family-dispute-resolution`
3. For each page whose content includes internal notes, set status to `Draft`.
4. Do not delete pages.
5. Clear cache.
6. Recheck the public URL in an incognito/private window.

## Verification
Each URL should meet one of these conditions:

- It no longer returns the generated internal article.
- It is restored to a previous clean page.
- It redirects/404s without internal notes.
- It does not contain:
  - `NOT VERIFIED`
  - `project-control`
  - `Source audit`
  - `סטטוס לפני פרסום`
  - `פעולות המשך`
  - `מבנה CMS`
  - `גרסת CMS`
  - `GSC`
  - `קניבליזציה`
  - `חסמי פרסום`

## After Cleanup
Do not republish these pages until:

1. `project-control/publication-cannibalization-check.csv` is approved per page.
2. Public body content is rewritten/cleaned.
3. Existing content is merged or redirected with approval.
4. Internal notes stay only in `project-control/`.
