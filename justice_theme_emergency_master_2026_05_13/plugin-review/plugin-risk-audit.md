plugin_name,status,purpose,risk,recommendation,safe_to_keep,safe_to_remove,requires_owner_approval,notes
justice-core,ACTIVE_IN_REPO,Core plugin: CPT registration for articles/lawyers/legal tools; REST endpoints; article-lawyer bridge; logger,MEDIUM - contains emergency-run.php which previously targeted all 1199 articles as family-law (BUG FIXED in review but not yet deployed),Keep but do NOT deploy until emergency-run.php is reviewed and confirmed safe. Remove emergency-run.php after confirming it does nothing harmful.,YES,NO,YES,The article-lawyer bridge and CPT registration are needed. The emergency-run.php file contained the family-law mass-tagging bug. Verify its current state before any deploy.
justice-core-v3,OLD_VERSION_IN_REPO,Old version of justice-core,LOW - just an old version sitting in the folder,Archive to arch-archive. Do not deploy.,NO,YES - archive only,NO,This is an old version. Do not confuse it with the active justice-core. Archiving recommended.
jt-deploy,DO_NOT_USE,Standalone deployment plugin created during emergency session,CRITICAL - known to contain logic that tags ALL 1199 articles as family-law regardless of actual content. This is the plugin that caused the classification crisis.,Do NOT upload. Do NOT activate. Delete or quarantine immediately.,NO,YES - quarantine,YES,This plugin is dangerous in its current form. The jt-deploy.zip in the root is also unsafe. Both must be quarantined. If a deployment helper is needed in the future, it must be rewritten from scratch with strict post-ID targeting.
CPT UI (if installed),UNKNOWN - not confirmed active on live site,Custom Post Type registration via UI,MEDIUM - if CPT UI is active AND justice-core also registers the same CPTs there will be duplicate registration conflicts,Verify if CPT UI is active on live site via wp-admin or REST API. If justice-core handles all CPT registration remove CPT UI.,UNKNOWN,UNKNOWN,YES,NEEDS VERIFICATION - this must be checked by logging into wp-admin
Rank Math (assumed active),ASSUMED ACTIVE - not confirmed via API,SEO plugin: meta titles descriptions canonical sitemap schema breadcrumbs,LOW - standard SEO plugin,Keep. Verify sitemap is generated and submitted. Verify Rank Math is generating valid schema.,YES,NO,NO,0 sitemaps found via GSC API. Rank Math should auto-generate. Verify at jus-tice.co.il/sitemap.xml immediately.
Cache plugin (unknown),UNKNOWN - not confirmed,Page caching,MEDIUM - cache has caused deployment issues in the past (emergency-run.php was blocked by cache),Identify which cache plugin is active. Ensure it can be purged via wp-admin or WP-CLI. Document cache-purge procedure for future deploys.,UNKNOWN,NO,YES,Cache was a documented blocker during the emergency plugin deployment sessions. Must be identified and controlled.
Security / WAF plugin (unknown),UNKNOWN,Security and WAF protection,MEDIUM - WAF has blocked PHP execution of admin requests in the past,Identify security plugin. Ensure REST API endpoints are not blocked for authenticated requests.,UNKNOWN,NO,YES,WAF blocking was a documented issue. uPress also has server-level WAF. Both must be understood before any API-driven bulk update.
Redirect plugin (unknown),UNKNOWN - not confirmed,301/302 redirect management,LOW if Rank Math handles it; MEDIUM if standalone,Identify. If Rank Math handles redirects use only Rank Math. Avoid running two redirect plugins simultaneously.,UNKNOWN,UNKNOWN,YES,No redirect plugin confirmed. Rank Math has redirect module. This must be confirmed before any redirect import.
WP All Import (if installed),UNKNOWN,Bulk content import,HIGH - bulk import without dry run and backup can cause mass content corruption,Do NOT run WP All Import for bulk changes without: (1) full database backup (2) dry run (3) owner approval (4) rollback plan. Prefer REST API for targeted updates.,UNKNOWN,NO,YES,Listed as risk. Safer alternative is REST API batch updates by post ID with logging.
