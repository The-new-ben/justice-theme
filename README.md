# Jus-Tice Emergency Content Master Control Folder

Generated: 2026-05-13 09:48:22
Generated by: GPT project organizer

## Purpose
This folder is a single GitHub-ready control structure for the Jus-Tice content/SEO/URL migration project.

It consolidates the accessible uploaded CSV/MD data into one starter master file and defines exactly what is missing before any safe content upload or URL migration.

## Honesty / limitation
The accessible uploaded CSV files were processed directly. The uploaded RAR archives were listed, but not extracted here because the runtime has no working `unrar`/`7z` backend. The RAR manifest proves that important files exist inside the archives, especially `content-master/content-bodies/`. Extract them locally and copy them into this structure before import decisions.

## Do not do yet
- Do not publish/import content.
- Do not change live URLs.
- Do not run redirects.
- Do not delete/noindex old content.
- Do not upload importer scripts until this master file is verified.

## Main files
- `content-master/master-content-database.csv`
- `content-master/master-content-database.xlsx`
- `content-master/missing-data-request.csv`
- `content-master/content-gap-map.csv`
- `content-master/cluster-priority-plan.csv`
- `content-master/redirects/redirect-map-template.csv`
- `agent-workflow/AGENT_RULES.md`
- `agent-workflow/PROMPT_TO_AGENT.md`
