# FULL HANDOFF — 2026-07-19 (from the remote Claude Code session to the local browser-enabled session)

## URGENT SEO RECOVERY UPDATE — 2026-08-26

Before touching redirects, automatic content, content clusters, sitemaps, or
410 responses, read `.project-control/seo-recovery-phase1-2026-08-26.md`.
Phase 1 has a reversible automatic-content freeze, a confirmed self-redirect
fix, a deterministic pillar resolver, a protected money-page allowlist, and a
40-URL 410 gate that must not publish until CRM, server logs, and a full host
backup are verified. The external evidence directory and exact rollback steps
are recorded there.

Read this whole file before touching anything. You are taking over a running
product with live customers-to-be. CLAUDE.md at repo root is LAW — read it
second. This file is the operational state + doctrine.

## 0. The three mandatory modes (owner doctrine — never optional)

- **GOD MODE** = evidence over claims, always: never report anything done
  without verifying the RENDERED live page (skill: `live-verify` — 7 cache
  layers, uPress SeoEdge needs `curl -X PURGE <url>` per URL); benchmark
  against competitors before/after; QA gauntlets on content; loud failure
  over silent fallback; design changes need real screenshots at 390px+1440px
  (you HAVE browser access — use it; the remote session could not).
- **360 MODE** = full-perimeter thinking on every task: data, SEO, design,
  business, legal/ethics, performance, caching, security — check what the
  change touches beyond the ask.
- **NON-PROMPTED RESPONSIBILITY** = you MUST surface and (when safe) fix
  things the owner did not ask for, every turn. You are responsible for the
  product, not the ticket. Real examples that mattered: a hidden 500-pin cap
  silently hid half the map; per-publish emails caused a host-wide mail
  block during a bulk import (~637 emails, uPress capped at ~300/day);
  source data carried 82 hidden duplicate pairs; internal billing jargon
  ("invoice_sent") rendered on a customer page. Bring at least this class of
  finding forward yourself, always.

## 1. What we are building (business)

jus-tice.co.il = the best legal website in Israel (owner: Ben Batash,
mistabrajustice@gmail.com). Engine: a full index of Israeli law firms
(~1,180 live records imported 2026-07-18 from a private ranking-guide
collaboration) → firms discover themselves as **mute dots on the 3D map**
and thin "public cards" → **register/claim** (one click from every dot
popup and profile banner) → registered card (visible label on map, richer
profile) → paid tiers (priority placement in the article strips + map) →
AI practice tools (Hebrew whitespace — no dominant Hebrew Harvey exists;
price anchor Cligal ₪74-158/mo, flat SaaS only, NEVER per-document — Avvo
was killed by US bar ethics for monetizing delivery) → courtroom-simulation
integration as the crown feature. FOMO ladder: dot → כרטיס רשום → מאומת →
מדורג (reviews/score).

**VOCABULARY LAW (owner order 2026-07-19): public labels never say
מקודם/ממומן — always the registered-card model: "כרטיס רשום". Lawyers must
see status they can obtain, not our ads at their expense.**

**PRIVACY LAW: ranking-guide identities (internally coded G1/G2/G3) are
NEVER named publicly. `jt_rank_entry` meta and
`project-control/lawyer-index/rankings-latest.csv` (perms 600) are internal
only. `priority_score` stays 0 unless a real paid plan sets it.**

## 2. Repos, folders, credentials

- **The-new-ben/justice-theme** — this repo. Work branch:
  `claude/seo-content-strategy-prompt-00ry0j` (all work committed+pushed
  there; owner deploys THEME files by "Pull Git" in the uPress panel —
  after any theme change, tell him to pull + Ctrl+Shift+R).
- **The-new-ben/agent-skills** — central PRIVATE skills repo. MANDATORY:
  after ANY change under `.claude/skills/`, run its
  `tools/sync-from-repo.sh /path/to/justice-theme justice-theme` and push.
  Knowledge accumulates there for every future agent — skills must keep
  evolving; when you learn a reusable lesson, write/extend a skill and sync.
  Check the repo for existing skills before inventing approaches.
- **The-new-ben/courtai** — HADMAIA simulation (see §6).
- **Credentials**: `.env` in the repo working folder (WP_BASE_URL, WP_USER,
  WP_APP_PASSWORD app-password for REST, SerpApi key). NEVER committed,
  NEVER printed. Load: `set -a; source .env; set +a`.
- **Reports/records**: `project-control/` (this file, lawyer-index/ with
  batches+QA+JOIN reports, rebuild-plan-2026-07/, courtai-integration-plan.md,
  nextjs-integration-contract-2026-07.md). Skills: `.claude/skills/`.

## 3. Deploy pipeline (skill: wp-deploy)

- THEME (root php, inc/, template-parts/, assets/css): owner pull only.
- PLUGIN justice-ops (the agent's channel, live v2.35.0): bump version in
  `justice-ops/justice-ops.php` (header + JUSTICE_OPS_VERSION) + changelog
  in `plugin-dist/justice-ops.json` → `python3 scripts/deploy/build-ops-zip.py X.Y.Z`
  → create temp Code Snippet (POST `/wp-json/code-snippets/v1/snippets`)
  exposing a `jt-tmp/v1/` route that Plugin_Upgrader-installs the base64
  zip → call it → verify `/wp-json/justice-ops/v1/healthcheck` → deactivate
  snippet (PUT active:false) → PURGE → rendered-verify. Snippets don't
  hard-delete via REST; deactivation is the control.
- Plugin `ultra-justice-engine` (v1.0.1) owns the justice_lawyer CPT/meta
  (`build-uje-zip.py`). `justice-core/` is repo-only — NEVER activate.
- Hard-won gotchas: `register_rest_route` override needs 4th arg `true`;
  `/lawyers/` and the front page are render-and-exit controlled routes —
  hook output buffers at `template_redirect` priority **-1000010**, normal
  hooks/queries lie there; client timeouts don't kill server PHP (poll
  X-WP-Total until stable, NEVER re-fire bulk imports); Autoptimize absorbs
  inline styles/scripts (verify inside aggregates + decode base64 data-URIs).

## 4. Live systems shipped this wave (all verified rendered)

Map v3 (`justice-ops/map-feed-v3.php` + `assets/cinema-map.js`): 967
offices; free = dots (click → popup + "כרטיס רשום באזור" nearest-registered
interception ≤3km + one-click claim CTA), registered/paid = labeled gold
flag (pulses on nearby dot hover); map opens `/lawyers/` under the H1 and
sits after the homepage hero. Article strip (`firm-match-strip.php`): 3
gated matched firms before first H2 on every article, priority-first +
daily rotation. Profiles: claim banner + no-login opt-out form + composed
facts; empty panels and "unknown" purged. Publish-notify email has a pause
route (`justice-ops/v1/publish-notify-pause`) — pause during ANY bulk
publish, ALWAYS restore after. Dedupe: name-key strips honorifics, dashes,
commas, periods; 82 verified duplicate pairs already merged+trashed —
verify by identical address+phone+website before ever trashing.

## 5. Competitors (side-by-side is a standing duty, especially language)

Israeli: **din.co.il** (directory UX), **psakdin.co.il** (content+cards),
**LawReviews** (reviews/rankings FOMO), **טקדין/Cligal** (AI, pricing
anchor), duns100 (refer to internally only as the guide partner — never
publicly). US patterns: **Avvo** (score+reviews+badge flywheel), Justia.
Before shipping ANY customer-facing copy (registration, plans, dashboards),
open the competitors' equivalent pages in the browser and match register:
benefits-first, trust, no internal jargon. The registration page was just
rewritten (theme — needs owner pull); continue that standard everywhere.

## 6. HADMAIA / courtai simulation (the crown)

Repo truth (verified): **Vite+React SPA (NOT Next.js)**, Netlify, Supabase
edge functions + Express payments, deployed on jus-tice.com; arena at
`/#/hadmaia`; **embed+postMessage already implemented** (`?embed=1&host=`,
`hadmaia:ready/resize`, `public/embed.js`); WP already iframes it at
`/legal-simulation/`. Data: one-line intake → LLM world-gen (Groq/Gemini/
OpenAI/Anthropic; move the client-side Groq key behind `arena-llm-gateway`).
Verified-professionals model exists (`docs/schemas/professional.json`).
**Gap = counsel-of-record**: no link between a verified real firm and an
arena participant ("does not act as counsel of record" is explicit). Visual
gap: arena is a 2D avatar grid; a real Three.js `VirtualCourtroom3D.tsx`
exists but is NOT wired into the arena. Why we'll be first: nobody in
Israel has directory+AI tools+visual simulation in Hebrew as one funnel.
Plan: (A) signed short-lived token endpoint on WP (`justice/v1/sim-token`)
minting case-context + firm identity for claimed-AND-consented registered
firms → sim renders "מיוצג על ידי X"; (B) serve the SPA at
sim.jus-tice.co.il with shared branding; consent + Bar-ethics gate before
any real firm name touches a simulated outcome. Sim-interest checkbox
already collects demand (`sim_representation_interest` meta).

## 7. Open items (owner-approved direction, execute under the modes)

1. Owner must pull git (registration-copy rewrite + accumulated theme fixes).
2. Homepage: move lawyer carousel directly under the map band (owner
   inclination confirmed; screenshot-verify at 390/1440 — you can).
3. Verification automation: Bar-card photo upload → OCR via existing
   ai-engine → cross-check Israeli Bar public registry → auto
   verification_status; human on exceptions only.
4. Ranking/reviews/FOMO: transparent Jus-Tice score (registration+
   verification+responsiveness+content — never guide data), embeddable
   badge (Avvo backlink flywheel), post-lead WhatsApp review requests via
   existing reviews-engine.
5. Registration form: duplicate prefilled name field; noindex registration/
   claim pages; profile header shows firm name twice.
6. Claim outreach to ~1,180 firms ONLY after external SMTP (uPress mail cap).
7. Junk taxonomy term id 731 (garbled name) — deletion awaits owner word.
8. Phase 4 AI tools + Phase 5 monetization wiring: designs recorded in
   project-control + git history (lawyer-plans ladder → clone
   advertise.php::justice_adv_activate pattern → priority_score/leads).
9. Sim phases A/B above.

Everything here was live-verified at handoff time. Trust nothing that you
have not re-verified rendered. Evolve the skills as you go — that is the law.
